import { Container } from "nuclui";
import React from "react";

const Benchmark = () => {
    return (
        <Container maxWidth="lg">
            <div>Benchmark</div>
        </Container>
    );
};

export default Benchmark;
